// POSPage - Point of Sale page wrapper
import React from 'react';
import { POSScreen } from '../../../components/pos';

const POSPage: React.FC = () => {
  return <POSScreen />;
};

export default POSPage;
